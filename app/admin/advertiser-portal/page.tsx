import AdvertiserPortalClient from "./AdvertiserPortalClient";

export const metadata = {
  title: "Advertiser Portal — FoodTrove Admin",
  description: "Self-serve advertiser performance portal — flight metrics, creative status, audience insights, and campaign recommendations",
};

export default function AdvertiserPortalPage() {
  return <AdvertiserPortalClient />;
}
