import DealDeskClient from "./DealDeskClient";

export const metadata = {
  title: "Deal Desk — FoodTrove Media",
  description:
    "Insertion order management — deal pipeline, IO line items, and Kevel flight tracking for all active and prospective advertisers.",
};

export default function DealDeskPage() {
  return <DealDeskClient />;
}
